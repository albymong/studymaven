<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studymaven.mapper.MemberMapper">
    <resultMap id="MemberResultMap" type="com.studymaven.domain.MemberVO">
        <id property="id" column="id" />
        <result property="userid" column="userid" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="role" column="role" />
        <result property="createDate" column="create_date" />
        <result property="updateDate" column="update_date" />
    </resultMap>

    <select id="selectByUserid" resultMap="MemberResultMap">
        SELECT * FROM member WHERE userid = #{userid} AND password = #{password}
    </select>

    <select id="selectByUseridOnly" resultMap="MemberResultMap">
        SELECT * FROM member WHERE userid = #{userid}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (userid, password, name, create_date, update_date)
        VALUES (#{userid}, #{password}, #{name}, NOW(), NOW())
    </insert>
</mapper>