<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studymaven.mapper.BoardMapper">
    <resultMap id="BoardResultMap" type="com.studymaven.domain.BoardVO">
        <id property="id" column="id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="createDate" column="create_date" />
        <result property="updateDate" column="update_date" />
    </resultMap>

    <select id="selectAll" resultMap="BoardResultMap">
        SELECT * FROM board ORDER BY id DESC
    </select>

    <select id="selectPage" resultMap="BoardResultMap">
        SELECT * FROM board ORDER BY id DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM board
    </select>

    <select id="select" resultMap="BoardResultMap">
        SELECT * FROM board WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board (title, content, create_date, update_date)
        VALUES (#{title}, #{content}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE board SET title = #{title}, content = #{content}, update_date = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM board WHERE id = #{id}
    </delete>
</mapper>